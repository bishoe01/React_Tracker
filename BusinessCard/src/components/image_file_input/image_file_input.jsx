import React from 'react';
import styles from './image_file_input.module.css';
function ImageFileInput({ name, onClick }) {
    return (
        <button>Image</button>
    );
}

export default ImageFileInput;